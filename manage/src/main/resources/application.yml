spring:
  datasource:
    url: jdbc:mysql://localhost:3306/manage
    username: KPlum
    password: 5tar0cean!FFVII
  jpa:
    database-platform: org.hibernate.dialect.MySQL8InnoDBDialect
    generate-ddl: true
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        globally_quoted_identifiers: true
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
  sql:
    init:
      mode: always # never, always, embedded, embedded-continue-on-error change to always to create tables 
      continue-on-error: false